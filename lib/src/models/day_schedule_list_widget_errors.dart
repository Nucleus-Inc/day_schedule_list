enum DayScheduleListWidgetErrors {
  /// It is not possible to create an appointment at this location,
  /// because the available time interval is less than
  /// the minimum allowed.
  unavailableIntervalToAddAppointment
}